<!-- refactored skeleton for anthropic_alfworld-object-placer (1 of 1 units replaced by child invocations (cleanup applied)) -->

# Instructions
invoke(locate-target-object, {target_object="{obj}", suspected_receptacle="{recep}"})  (parent-specific: 1. **Verify Prerequisites**:
   - Confirm you are currently holding the target object (`{obj}`).
   - Confirm the target…)